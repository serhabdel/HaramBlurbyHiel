<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Required permissions for HaramBlur functionality -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Location permissions for prayer times -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Notification permission for Android 13+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Optional permissions for enhanced app blocking capabilities -->
    <!-- These permissions enhance blocking capabilities but are not required for basic functionality -->
    <!-- The app remains fully functional without them, providing progressive enhancement -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" tools:ignore="ProtectedPermissions" />
    
    <!-- Android 11+ Package Visibility Permissions -->
    <!-- Required to detect installed apps on Android 11 (API 30) and higher -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" tools:ignore="QueryAllPackagesPermission" />
    
    <!-- Alternative: Specific package queries for major social media apps -->
    <!-- More restrictive approach if QUERY_ALL_PACKAGES is not accepted -->
    <queries>
        <!-- Messaging Apps -->
        <package android:name="com.whatsapp" />
        <package android:name="com.whatsapp.w4b" />
        <package android:name="org.telegram.messenger" />
        <package android:name="com.discord" />
        <package android:name="com.viber.voip" />
        <package android:name="org.thoughtcrime.securesms" />
        <package android:name="com.tencent.mm" />
        <package android:name="jp.naver.line.android" />
        <package android:name="com.kakao.talk" />
        <package android:name="com.facebook.orca" />
        
        <!-- Social Media Apps -->
        <package android:name="com.instagram.android" />
        <package android:name="com.facebook.katana" />
        <package android:name="com.twitter.android" />
        <package android:name="com.snapchat.android" />
        <package android:name="com.zhiliaoapp.musically" />
        <package android:name="com.ss.android.ugc.tiktok" />
        <package android:name="com.google.android.youtube" />
        <package android:name="com.linkedin.android" />
        <package android:name="com.reddit.frontpage" />
        <package android:name="com.tumblr" />
        <package android:name="com.pinterest" />
        
        <!-- Dating Apps -->
        <package android:name="com.tinder" />
        <package android:name="com.bumble.app" />
        <package android:name="com.okcupid.okcupid" />
        <package android:name="com.hinge.app" />
        
        <!-- Video Platforms -->
        <package android:name="tv.twitch.android.app" />
        <package android:name="tv.danmaku.bili" />
        
        <!-- Regional Social Media -->
        <package android:name="com.vkontakte.android" />
        <package android:name="ru.ok.android" />
        <package android:name="org.bluesky.bsky" />
        <package android:name="org.joinmastodon.android" />
        <package android:name="com.bereal.ft" />
    </queries>

    <application
        android:name=".HaramBlurApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HaramBlur">
        
        <!-- ML Kit Face Detection - ensure model is available -->
        <meta-data
            android:name="com.google.mlkit.vision.DEPENDENCIES"
            android:value="face" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- HaramBlur Accessibility Service -->
        <service
            android:name=".accessibility.HaramBlurAccessibilityService"
            android:exported="false"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <receiver
            android:name=".accessibility.AccessibilityServiceRestartReceiver"
            android:exported="false" />

        <!-- HaramBlur Device Admin Receiver for enhanced app blocking -->
        <!-- This enables force-closing of blocked apps for stronger enforcement -->
        <!-- Device Admin access is optional and enhances blocking capabilities -->
        <receiver
            android:name=".detection.HaramBlurDeviceAdminReceiver"
            android:exported="true"
            android:enabled="true"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
                <action android:name="android.app.action.DEVICE_ADMIN_DISABLED" />
            </intent-filter>
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin_config" />
        </receiver>

        <!-- WorkManager initialization provider -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <!-- FileProvider for sharing log files -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>

        <!-- Dhikr Notification Receiver -->
        <receiver
            android:name=".services.DhikrNotificationReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.hieltech.haramblur.DHIKR_DISMISS" />
                <action android:name="com.hieltech.haramblur.DHIKR_NEXT" />
                <action android:name="com.hieltech.haramblur.DHIKR_SHOW_NOW" />
            </intent-filter>
        </receiver>

        <!-- Usage Time Notification Receiver -->
        <receiver
            android:name=".services.UsageTimeNotificationReceiver"
            android:exported="false"
            android:enabled="true">
            <intent-filter>
                <action android:name="com.hieltech.haramblur.REFLECT_AND_CONTINUE" />
                <action android:name="com.hieltech.haramblur.CLOSE_APP" />
                <action android:name="com.hieltech.haramblur.OPEN_SETTINGS" />
                <action android:name="com.hieltech.haramblur.DISMISS_USAGE_NOTIFICATION" />
                <action android:name="com.hieltech.haramblur.SHOW_GUIDANCE" />
            </intent-filter>
        </receiver>

        <!-- Prayer Notification Receiver -->
        <receiver
            android:name=".services.PrayerNotificationReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.hieltech.haramblur.PRAYER_COMPLETED" />
                <action android:name="com.hieltech.haramblur.PRAYER_NOT_COMPLETED" />
                <action android:name="com.hieltech.haramblur.PRAYER_WILL_DO_NOW" />
                <action android:name="com.hieltech.haramblur.PRAYER_ALREADY_DONE" />
                <action android:name="com.hieltech.haramblur.SHOW_QURANIC_GUIDANCE" />
            </intent-filter>
        </receiver>

        <!-- Reflection dialog activity opened from notifications -->
        <activity
            android:name=".ui.ReflectionActivity"
            android:exported="false"
            android:theme="@style/Theme.HaramBlur">
        </activity>

        <!-- Diagnostics activity for troubleshooting -->
        <activity
            android:name=".ui.DiagnosticsActivity"
            android:exported="false"
            android:theme="@style/Theme.HaramBlur"
            android:label="@string/diagnostics_title">
        </activity>

        <!-- Islamic Dashboard Widget -->
        <receiver
            android:name=".widget.IslamicDashboardWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_islamic_dashboard_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.hieltech.haramblur.widget.INCREMENT_TASBIH" />
                <action android:name="com.hieltech.haramblur.widget.RESET_TASBIH" />
                <action android:name="com.hieltech.haramblur.widget.UPDATE_WIDGET" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/islamic_dashboard_widget_info" />
        </receiver>

        <!-- Dhikr Counter Widget -->
        <receiver
            android:name=".widget.DhikrCounterWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_dhikr_counter_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.hieltech.haramblur.widget.DHIKR_INCREMENT" />
                <action android:name="com.hieltech.haramblur.widget.DHIKR_RESET" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/dhikr_counter_widget_info" />
        </receiver>

        <!-- Prayer Times Widget -->
        <receiver
            android:name=".widget.PrayerTimesOnlyWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_prayer_times_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/prayer_times_widget_info" />
        </receiver>

    </application>

</manifest>
